services:
  claude-observe:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      SERVER_PORT: "4001"
      DB_PATH: /data/events.db
    ports:
      - "${SERVER_PORT:-4001}:4001"
      - "${CLIENT_PORT:-5174}:5174"
    volumes:
      - ./data:/data
